{
	"name": "jj (Jujutsu)",
	"id": "jujutsu-cli",
	"version": "1.0.0",
	"description": "Installs jj (Jujutsu), a Git-compatible version control system.",
	"documentationURL": "https://github.com/raftergoose/devcontainer-features/tree/main/src/jujutsu-cli",
	"options": {
		"version": {
			"type": "string",
			"proposals": [
				"latest",
				"0"
			],
			"default": "latest",
			"description": "Select version of jj."
		},
		"completionMode": {
			"type": "string",
			"proposals": [
				"dynamic",
				"standard",
				"none"
			],
			"default": "dynamic",
			"description": "Select shell completion mode for bash/zsh/fish. 'dynamic' is recommended by jj docs."
		},
		"configureUserFromGit": {
			"type": "boolean",
			"default": true,
			"description": "If true, read git global user.name/user.email and set jj user.name/user.email (skips if jj user config already exists)."
		},
		"configureEditor": {
			"type": "boolean",
			"default": true,
			"description": "If set to true, configures jj to use VS Code as its default editor."
		}
	},
	"installsAfter": [
		"ghcr.io/devcontainers/features/common-utils",
		"ghcr.io/meaningful-ooo/devcontainer-features/fish"
	],
	"postStartCommand": {
		"jujutsu-configure-user-from-git": "/usr/local/share/jujutsu-cli/scripts/configure-user.sh",
		"jujutsu-configure-editor": "/usr/local/share/jujutsu-cli/scripts/configure-editor.sh"
	}
}
